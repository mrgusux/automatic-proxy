# Snapshot fixtures

This directory holds dummy/static proxy payloads used for snapshot testing the
extractors against known inputs. Add `.html`, `.json`, or `.txt` fixtures here and
reference them from `tests/unit/test_extractors.py`.
